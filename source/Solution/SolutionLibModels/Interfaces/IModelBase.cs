namespace SolutionModelsLib.Interfaces
{
    /// <summary>
    /// This interface provides a base interface for all model items in the solution,
    /// including the root item, all items below it, and the solution root.
    /// </summary>
    public interface IModelBase
    {
    }
}